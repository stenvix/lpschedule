#!/bin/bash
source $OPENSHIFT_PYTHON_DIR'virtenv/bin/activate';
cd $OPENSHIFT_REPO_DIR;
celery multi start worker1 -A schedule.celery -B -l info -Ofair --logfile=$OPENSHIFT_LOG_DIR"celery.log";
echo "Celery successfully launched"
